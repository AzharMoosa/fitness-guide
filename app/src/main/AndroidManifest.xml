<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
  package="com.example.fitnessapp">

  <application
    android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:roundIcon="@mipmap/ic_launcher_round"
    android:supportsRtl="true"
    android:theme="@style/Theme.FitnessApp"
    android:usesCleartextTraffic="true">
    <activity android:name=".chat.ChatRoom"></activity>
    <activity android:name=".settings.About" />
    <activity android:name=".settings.HealthInformation" />
    <activity android:name=".settings.ChatSettings" />
    <activity android:name=".settings.UserSettings" />
    <activity android:name=".routines.EditRoutine" />
    <activity android:name=".routines.CreateRoutine" />
    <activity android:name=".routines.ViewRoutine" />
    <activity android:name=".routines.Routines" />
    <activity android:name=".Dashboard" />
    <activity android:name=".auth.SignUp" />
    <activity android:name=".auth.Login" />
    <activity android:name=".MainActivity">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>

    <meta-data
      android:name="preloaded_fonts"
      android:resource="@array/preloaded_fonts" />
  </application>
  <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

  <uses-permission android:name="android.permission.INTERNET" />

</manifest>